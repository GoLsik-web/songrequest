songrequest.exe
songrequest
*.db
*.db-wal
*.db-shm
*.log
*.exe~
dist/

# Рабочие заметки: передача дел между сессиями. Нужны только на этой машине.
ПРОДОЛЖИТЬ.md

# Логи и выгрузки с чужих машин. Там ники зрителей, история заказов и
# опознавательные номера приложений — в общий доступ им нельзя.
docs/лог-тестера-*/
docs/логи-*/
